#!/usr/bin/env bash
set -euo pipefail

dashy_root="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
cd "$dashy_root"

if [[ ! -f dist/index.html ]]; then
  npm run build
fi

exec node server/index.js "$@"
